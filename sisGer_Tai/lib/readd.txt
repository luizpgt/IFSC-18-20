adicionando ao git

para clonar:

dentro da pasta que quiser:

::git clone LINK::

para atualizar o codigo que esta na sua maquina com o que tem no git: 

::git pull origin::

para fazer um envio ao git:

::git add --all::
::git commit -m "escreva o pq da mudanca"::
::git push origin master::
